import React from 'react'
import useModal, {Provider} from 'user-react-modal'
const Modal = () => {

    const { openModal, closeModal, isOpen, Modal } = useModal({
       
      });
    return (
        <div>
            
        </div>
    )
}

export default Modal
